# password_strength_checker
HTML script that can be run from a local directory and prompts the user to enter a password and spit out a strength score. 

Pretty simple here, folks. Run the script (browser) and enter a password. 
